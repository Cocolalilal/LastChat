- Prefers new data/state transformations implemented as pure file-level or extension functions — following the codebase's existing "testable-helper" pattern — instead of inline logic inside controllers/services, so they can be unit-tested directly; e.g. iOS message edit/delete/fork extracted into withEditedMessage/withDeletedMessage/buildIosForkConversation, each with commonTest coverage. Confidence: 0.8
- Prefers portable pure-Kotlin implementations over platform-specific cinterop/expect-actual when a needed platform library is missing — e.g. when Kotlin/Native had no SQLite platform klib for Apple targets, chose writing a pure-Kotlin read-only SQLite file reader (B-tree walks, record serial types, overflow chains, rowid aliases) over vendoring cinterop headers, explicitly for "no cinterop, no platform dependency" and commonTest runnability on every target. Confidence: 0.8
