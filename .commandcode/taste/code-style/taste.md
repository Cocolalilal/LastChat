- Prefers new data/state transformations implemented as pure file-level or extension functions — following the codebase's existing "testable-helper" pattern — instead of inline logic inside controllers/services, so they can be unit-tested directly; e.g. iOS message edit/delete/fork extracted into withEditedMessage/withDeletedMessage/buildIosForkConversation, each with commonTest coverage. Confidence: 0.8
