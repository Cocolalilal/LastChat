- Works across multiple AI coding agents (Codex, Claude Code, Antigravity) and expects cross-agent taste learning — wants Command Code to recognize and import sessions from other agents to learn usage patterns; reaffirmed the expectation explicitly for Codex and Claude Code when told the mechanism should work, i.e. it must actually function, not just be advertised; proactively supplies other agents' session/chat IDs (e.g. an Antigravity chat id) and expects the agent to locate and mine those chat records to recover the original requirements before acting. Confidence: 0.95
- Prefers a "plan first" workflow before large feature implementation — repeatedly asks the agent to produce a complete plan and get approval before writing code, especially for complex subsystems like memory, context management, and UI overhauls. Confidence: 0.95
- Encourages and explicitly allows subagent delegation for research and parallel work — expects agents to use subagents (preferring Opus/Sonnet models) for deep research tasks, codebase mapping, and background compilation. Confidence: 0.9
- Prefers reusing existing components and wiring them up rather than building replacements from scratch — e.g. reusing the existing message input bar in new contexts, pulling code from RikkaHub for OAuth fixes. Confidence: 0.85
- Prefers reverting entire broken approaches and starting fresh rather than incrementally patching a flawed system — has reverted multiple memory system implementations to start over with a better plan; treats reverts as a clean-up tool, not a failure. Confidence: 0.85
- Wants commits at logical checkpoints (before and after major changes) and values the ability to review what was done through commit summaries — commits use conventional style with scoped, detailed bodies (feat(ios):, fix(tts):), unrelated fixes get their own commit, and messages are written to scratchpad files for `git commit -F` (cmd has no heredocs). Confidence: 0.8
- Welcomes pushback and alternative suggestions from agents — explicitly says "you can go against if you think we shouldn't do it" and "you can talk against me on this"; values agents that think critically about requirements rather than blindly implementing. Confidence: 0.8
- Prefers core logic source-shared between platforms so both execute literally the same code, and ports Android semantics verbatim — first reads the Android reference implementation (ChatVM, ChatService, message-action UI) to capture exact behavior, then mirrors it in the iOS port; identical merge/version-selection/deletion semantics are treated as the definition of "behaving identically". Confidence: 0.9
- Drives the iOS port from a maintained incremental ledger: tasks carry landed-vs-remaining notes, next work is picked by dependency order, each session lands one verifiable increment and updates the ledger plus project status docs (docs/ios-portability.md); the user's "Continue" means resume the ledger from where the last session stopped. Confidence: 0.85
- Before committing, runs a full compile/test gate matrix across all affected targets (:ai, :app, :tts, :ui-core, all three iOS Kotlin targets) and greps build logs for failures; iOS commonTests are only compiled on the Windows machine and are expected to execute on macOS CI. Confidence: 0.85ng and fixing compile errors afterward. Confidence: 0.7
- Verifies risky low-level/parsing code by actually executing it against real fixtures, not just compiling — generates a fixture with the production schema (e.g. a real 24 KB SQLite DB with the Android entity tables and a >4 KB payload spanning overflow pages, committed as base64 for commonTest) and additionally compiles a standalone Kotlin/Native executable with kotlinc-native from the .konan distribution to run the code on the dev machine; that runtime run caught a real bug (INTEGER PRIMARY KEY rowid-alias reading back as NULL) that compile gates alone would not. Confidence: 0.85
- Complex inline `python -c` one-liners with nested quoting fail on Windows — after repeated failures, writes helper Python scripts to the commandcode scratchpad and runs them by path instead. Confidence: 0.75