# iOS parity matrix

Android remains the acceptance reference. A row is complete only after both a
functional check and a screenshot comparison on compact and wide layouts.

| Surface | Shared/iOS foundation | Production data | Visual QA | Status |
|---|---:|---:|---:|---|
| App theme, shape, and font tokens | Shared shapes, typography, AMOLED transform, same Google Sans Flex TTF, and all six Android palettes | Persisted theme, system/light/dark mode, per-role fontSettings metrics, and always-on true-black dark surfaces | Pending Xcode | In progress |
| Chat shell and composer | Shared production grouped-bubble container, 48 dp add Surface, baseline row, 24 dp input capsule, outline tokens, five-line text geometry, nine-state Picker/Send/Loading/STT/questionnaire/tool action renderer, exact composer attachment strip, sent-message attachment row/tiles, and assistant-message TTS action used by Android and iOS | Persistent iOS conversations, attachment-only/text sends, native file selection, streaming, cancellation, TTS playback, OpenAI-compatible STT, and empty-state provider-settings handoff. DisplaySetting knobs now drive jumper, blur, code wrap/collapse, context stacks, new-chat GREETING/BIG_ICON/TEMPLATES/ACTIONS, generation haptics, real user/assistant avatars, token/reasoning chrome, toolbar placement, STT-replaces-icon, TTS filters, fonts, and assistant UI overrides. Composer Picker opens the in-chat model sheet. | Pending Xcode | In progress |
| Conversation menu | Shared Android modal drawer sheet, expandable search control, grouped quick-action rows and search animation, circular action Surface/press physics, BarChart/Settings glyphs, footer pill geometry, assistant picker sheet, selected paint, grouped corner animation, and row spacing | App-container persistence, title filtering, select/create/rename/delete, drawer lifecycle, statistics/settings navigation, assistant-to-conversation handoff, and a user nickname/avatar picker in the drawer header | Pending Xcode; Imagine remains until iOS image generation is functional | In progress |
| Top-app-bar navigation | Shared Android back control plus the outlined 48 dp chat drawer control and rounded Menu glyph | Native route callbacks and Pop haptic semantics | Pending Xcode | In progress |
| Statistics | Shared production stat-card and activity-heatmap implementations | Persisted conversation, message, input, output, cached-token totals and heatmap days via `PortableConversationStore` (`observeUsageTotals` / `observeDailyActivity`). Shared 12-month semantics: live chats supply conversation/token counts; message count is max(heatmap, live messages) so deleted chats still paint the heatmap. | Pending Xcode | In progress |
| Settings shell | Shared production compact groups, input cards, form rows, Android's exact compact Providers/Search/TTS selector, and its 336 dp adaptive pane at the 840x600 breakpoint, including full compact/wide hierarchy, expandable children, selected paint, corner morphing, row heights, press physics, and timing | Provider, search, TTS, STT, appearance, assistants, prompt injections, MCP, web server, backup/WebDAV, chat storage, overlay, and developer destinations persist. Display persists the Android DisplaySetting keys for notifications, GitHub update checks, new-chat-on-start, TTS autoplay, jumper, blur, code wrap, context stacks, header/content styles, generation haptics, avatars, token/reasoning chrome, toolbar-at-bottom, and STT-replaces-icon. Live `fetchGithubLatestRelease` uses `PlatformHttpClient`. Display also applies `userAvatar`/`userNickname`, per-role `fontSettings`, `ttsTextFilterRules`, `providerViewMode`, assistant `uiSettings`, and always-on AMOLED dark surfaces. Unported Android-only Assist/PRoot details stay explicit | Pending Xcode; overlay Assist activity is iOS App Intent + in-process overlay | In progress |
| About | Shared Android production body and exact launcher asset, including logo footprint, version pill, grouped rows, press spring, and credits | Live platform version (`1.4.7`), device, CPU architecture, source URL opening, platform haptics, and a GitHub latest-release pill/dialog gated by `checkForUpdates` | Pending Xcode | In progress |
| Default model and provider configuration | Shared production model-feature card and grouped picker rows, including animated selected-pill geometry | Three chat providers, retained per-provider endpoint/model settings, searchable persisted default-model selection, active generation routing, and per-provider Keychain secrets. iOS Display can refresh `lastchat_catalog.json` through shared `ModelCatalogService` + `mergeCatalogIntoProviders` | Pending Xcode; provider icons still depend on bundled catalog assets | In progress |
| Streaming generation | `PortableChatEngine` / `PortableGenerationLoop` is the single stream+tool loop. `PortableGenerationPrepare` owns lorebook/skills/RAG/smart-context, image-archive/OCR preprocess, recent-chat memories, `renderMessageTemplate` (if/elseif/filters/comments), and workspace reminders. `assemblePortableTools` is the live tool list. Android ChatService/UI conversation get/save/delete/undo/pin/title **and** paging/FTS/usage/web lists go through `PortableConversationStore` (`RoomPortableConversationStore` SQL LIMIT/OFFSET + Room usage DAO); iOS uses `JsonFilePortableConversationStore` in-memory filter/sort. iOS OCR uses Vision. `PortableTaskScheduler` owns spontaneous/scheduled/memory/**chat storage maintenance** jobs (WorkManager on Android, BGAppRefresh + in-process fallback on iOS). | OpenAI-compatible, Google, and Claude chat wired; cancellation, persistence checkpoints, and the standard model-driven tool-call/result loop | No | In progress |
| Attachments and media | Native media/document picker, sandbox copy, provider media encoder, Coil image rendering, shared production 84 dp composer strip, shared full-width sent-message row with 72 dp tiles, portable attachment-opening contract, and inline AVAudioPlayer for audio parts | Pending drafts and sent media persist; UIKit preview/Open In is wired for sent tiles; Android crop/URI cleanup, archived grayscale, zoom, and intents remain adapter-owned | Pending Xcode | In progress |
| Backup and restore | Shared zip reader/writer plus portable WebDAV over PlatformHttpClient | Android backup restore (providers, assistants, search, TTS, skills, lorebooks, MCP, WebDAV) plus iOS WebDAV upload/list/restore | Pending Xcode | In progress |
| Memory and RAG | Production memory mode/settings/row UI, temporal extraction contract/prompt, vector math, keyword scoring, and chunking are source-shared with Android | Persistent assistant-scoped core/episodic memories; Basic and hybrid Searchable recall; Adaptive evidence watermarks, 8-message/10-minute extraction trigger, structured claim lifecycle and episode upserts; `PortableTaskScheduler` plus native iOS BGAppRefresh durability with in-process fallback for memory, scheduled, spontaneous, and chat-storage-maintenance jobs; OpenAI/Google embeddings; memory tools; preferences contain no secrets; iOS Assistant settings expose `enableSpontaneous` plus hours/frequency/prompt | Pending Xcode scheduling simulation and live-provider extraction QA | In progress |
| Search providers | All services compile for iOS; Bing's portable client is installed and the real `search_web` tool is wired | Bing, SearXNG URL/auth, and 13 API-backed providers have persistent settings, Keychain-only secrets, configurable result limits, and model-driven execution | Pending Xcode | In progress |
| Cloud TTS | All eight cloud providers, source-shared queue/chunk/prefetch controller, AVFoundation audio player, and `PlatformSystemTts` (AVSpeech) compile for iOS; Android keeps Media3 plus Android `TextToSpeech` behind the same contracts | Provider-specific settings persist without credentials; cloud API keys remain Keychain-only; system TTS needs no key; assistant-message play/stop uses the same production action as Android | Pending Xcode | In progress |
| Local assistant tools | Android's production settings-group rows, normal model tool loop, questionnaire UI, exact image-tool schema, and workspace tool names are reused | Notification/TTS/scheduled/history/questionnaire paths are functional; `createPortableWorkspaceTools` is the shared loop for Android ChatService and iOS; PRoot is `AndroidBoundWorkspaceRuntime` | Pending Xcode | In progress |
| Image generator and gallery | Android's Imagine drawer entry, generator/gallery hierarchy, crossfade, empty state, 28 dp floating input surface, 20 dp media corners, aspect/count settings, and stop/generate interaction are reproduced in Compose Multiplatform | OpenAI-compatible/Google generation, cancellation, app-container persistence, preview opening, gallery grid, and deletion are functional; ComfyUI image-to-image uploads to `/upload/image` and injects `LoadImage` nodes from `ImageGenerationParams.inputImages`; iOS provider editor can add/configure ComfyUI (base URL, API workflow JSON import, prompt/model node mapping) | Pending Xcode animation/screenshot QA | In progress |
| Embedded web UI | Shared `PortableWebApiRouter` plus iOS Ktor CIO host; Gradle `prepareIosWebUi` builds the React SPA and copies it into the Xcode `webui` resource folder | Long-lived settings/conversation SSE, SPA mutations (edit/fork/regenerate/upload/tool-approval/pin/title/move/skills/settings), `/conversations`, `/paged`, and `/search` served from `PortableConversationStore` (not Room/`ConversationRepository`), fallback HTML client. While the Ktor host is bound, iOS posts a UserNotifications status on category `web_server` (`web_server:status`) and cancels it when the host stops | Pending Xcode browser QA | In progress |
| Share and widgets | `PlatformShareSheet` + `AssistantWidgetSnapshot` / `PlatformWidgetStore`; iOS UIActivityViewController, WidgetKit JSON snapshot, and Android ACTION_SEND / SharedPreferences adapters | Conversation export from the chat app bar; widget snapshot published to App Group `group.lastchat.rikkafork.cocolal`; Share Extension writes `pending_share_text` into that suite; Xcode targets `LastChatWidget` and `LastChatShareExtension` exist | Pending Xcode Simulator QA and signing with a development team | In progress |
| Assistant overlay | Shared `PortableSharePayload` plus in-process Compose overlay on iOS; Android keeps `AssistantOverlayActivity` | Overlay send uses the same `IosAppController.send` path; Siri App Intent `Ask LastChat`, `lastchat://overlay?text=`, long-press overlay icon, and App Group `pending_overlay_prompt` all present the overlay; launch consumes pending App Group share text | Pending Xcode | In progress |
| Local models/workspace | Shared `OnDeviceLlmProvider` + `OnDeviceLlmRuntime` / `OnDeviceWorkspaceRuntime` plus `PortableOnDeviceModelManager` for catalog/download/delete of LiteRT and Sherpa files. Android `GenerationHandler`/`ChatService` resolve LiteRT through `OnDeviceLlmProvider` (LiteRT is the runtime implementation, not a registered engine). Workspace tools use `createPortableWorkspaceTools`. | iOS Workspaces lists the same catalogs, streams downloads through `PlatformHttpClient.downloadTo`, unpacks Sherpa `.tar.bz2` required files via `PortableTarBz2` into `on_device_models/stt/{id}/`, and shows the shared unavailable runtime copy when inference cannot run. Live on-device inference remains Android LiteRT/PRoot | No | In progress |

## Acceptance rules

- Do not change Android spacing, typography, colors, shapes, motion, or haptics
  merely to accommodate iOS.
- Use Compose Multiplatform for shared UI. SwiftUI is only the native host.
- Keep platform-only capabilities behind contracts and show an explicit
  unavailable state until the iOS adapter exists.
- Never store provider secrets in preferences; the iOS implementation must use
  Keychain before provider configuration is enabled.
- Validate the Android app after every common UI extraction.
