<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration
    - Blocks cleartext (HTTP) traffic by default for security
    - Allows HTTP for localhost/127.0.0.1 to support local LLM hosts like Ollama
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    
    <!-- Allow HTTP for localhost (Ollama, local dev servers) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Android emulator host -->
    </domain-config>
</network-security-config>
